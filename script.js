document.addEventListener('DOMContentLoaded', () => {
    // The download functionality is temporarily disabled.
    // The download button is disabled in index.html and a message is shown to the user.
});
